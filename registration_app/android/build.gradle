// buildscript {
//     repositories {
//         google()
//         mavenCentral()
//     }
//     dependencies {
//         classpath 'com.android.tools.build:gradle:7.0.4' // Use the appropriate Gradle plugin version
//         classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.6.0" // Use the appropriate Kotlin plugin version
//         classpath "dev.flutter:flutter-gradle-plugin:latest.release" // Use the appropriate Flutter plugin version
//     }
// }

allprojects {
    repositories {
        google()
        mavenCentral()
    }
    
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
// implementation fileTree(dir: 'libs', include: ['*.jar'])
// implementation 'com.android.support:appcompat-v7:26.1.0'
// implementation 'com.android.support.constraint:constraint-layout:1.1.2'
// testImplementation 'junit:junit:4.12'